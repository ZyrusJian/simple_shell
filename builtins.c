#include "shell.h"

/**
 * find_builtin - finds matching builtin for input
 * @info: shell info struct
 *
 * Return: 1 if builtin found, 0 otherwise
 */

int find_builtin(info_t *info)
{
	/* loop through builtins */
	/* compare input to builtin names */
	/* if match, run builtin function */

	return (0); /* placeholder */
}
