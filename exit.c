#include "shell.h"

/**
 * exit_shell - exits the shell
 * @info: shell info struct
 *
 * Return: always 0
 */

int exit_shell(info_t *info)
{
	/* free allocated memory */
	/* close file descriptors */
	/* exit with info->status */

	return (0);
}
